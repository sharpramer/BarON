import { StyleSheet } from "react-native"

export const estilosMenus = StyleSheet.create({
    tituloMenussss: {
        flex: 1,
        alignItems: 'center',
        justifyContent: "center"
    },
    
});
